server {
    listen 80;
    server_name oneapi.gitagent.io;

    # 日志配置
    access_log /var/log/nginx/oneapi.gitagent.io.access.log;
    error_log /var/log/nginx/oneapi.gitagent.io.error.log;

    # 客户端最大请求体大小
    client_max_body_size 64m;

    location / {
        proxy_http_version 1.1;
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header Accept-Encoding gzip;
        proxy_read_timeout 300s;  # GPT-4 需要较长的超时时间，请自行调整
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
    }
}

# HTTPS 配置（使用 Let's Encrypt 证书后取消注释并修改）
# server {
#     listen 443 ssl http2;
#     server_name oneapi.gitagent.io;
#
#     ssl_certificate /etc/letsencrypt/live/oneapi.gitagent.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/oneapi.gitagent.io/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     # 日志配置
#     access_log /var/log/nginx/oneapi.gitagent.io.access.log;
#     error_log /var/log/nginx/oneapi.gitagent.io.error.log;
#
#     # 客户端最大请求体大小
#     client_max_body_size 64m;
#
#     location / {
#         proxy_http_version 1.1;
#         proxy_pass http://localhost:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header Accept-Encoding gzip;
#         proxy_read_timeout 300s;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 300s;
#     }
# }
#
# # HTTP 重定向到 HTTPS
# server {
#     listen 80;
#     server_name oneapi.gitagent.io;
#     return 301 https://$server_name$request_uri;
# }
